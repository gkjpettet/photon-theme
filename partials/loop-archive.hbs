<article class="gh-card {{post_class}}">
    <a class="gh-card-link" href="{{url}}">
        <header class="gh-card-header">
            <h2 class="gh-card-title">{{title}}</h2>
            <p><time class="archive-date" datetime="{{date format="YYYY-MM-DD"}}">{{date}}</time></p>
        </header>
    </a>
</article>